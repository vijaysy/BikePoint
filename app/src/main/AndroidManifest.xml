<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.Light"
        tools:targetApi="31" >
        <activity
            android:name=".admin.Admin"
            android:label="Admin Dashboard"
            android:exported="false" />
        <activity
            android:name=".admin.UpdateServices"
            android:label="Update Services"
            android:exported="false" />
        <activity
            android:name=".user.ServicesHistory"
            android:label="Services History"
            android:exported="false" />
        <activity
            android:name=".user.ServiceBooking"
            android:label="Booking Services"
            android:exported="false" />
        <activity
            android:name=".Register"
            android:label="Register"
            android:exported="false"
            android:theme="@style/Theme.Material3.Light" />
        <activity
            android:name=".user.UserActivity"
            android:label="User Dashboard"
            android:exported="false"
            android:theme="@style/Theme.Material3.Light" />
        <activity
            android:name=".Login"
            android:label="Login"
            android:exported="true"
            android:theme="@style/Theme.Material3.Light" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>